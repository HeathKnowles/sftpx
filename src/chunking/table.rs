// Chunk table and metadata

/// Table for storing chunk metadata
pub struct ChunkTable {
    // TODO: Implement chunk metadata storage
}

impl ChunkTable {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ChunkTable {
    fn default() -> Self {
        Self::new()
    }
}
